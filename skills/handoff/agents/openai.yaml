interface:
  display_name: "Handoff"
  short_description: "Resume repository work with evidence and ownership"
  default_prompt: "Initialise the handoff: use $handoff to claim a session name, audit this repository's ledger, and report what is actually unfinished."
